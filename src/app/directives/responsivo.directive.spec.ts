import { ResponsivoDirective } from './responsivo.directive';

describe('ResponsivoDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsivoDirective();
    expect(directive).toBeTruthy();
  });
});
